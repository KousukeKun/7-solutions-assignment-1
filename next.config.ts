import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  basePath: "/7-solutions-assignment-1",
  output: "export",  // enables static exports
  reactStrictMode: true,
};

export default nextConfig;
